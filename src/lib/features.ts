/**
 * Temporary UI toggles.
 *
 * SHOW_SCORES hides the points/score system everywhere (score header, per-task
 * +score/−bleed, task-detail score card, add-task live preview) without ripping
 * the logic out. Flip back to `true` to restore it.
 */
export const SHOW_SCORES = false
